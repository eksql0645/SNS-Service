const userModel = require('./user');
const postModel = require('./post');
const tagModel = require('./tag');
const commentModel = require('./comment');

module.exports = { userModel, postModel, tagModel, commentModel };
